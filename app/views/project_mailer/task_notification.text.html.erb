<html>
<body>
  <%=raw @task_details%>
  <% @task.attachments.each do |attach| %>
    <br/>
    <a href="<%= APP_CONFIG[:site_url] %>/file_download_from_email/<%=attach.id%>"><%= "#{attach.filename}" %></a> <br/>
  <%end %>
  <br/>
  Subscribed: <%= @people %><br/>
  <a href="<%= APP_CONFIG[:site_url] %>/unsubscribe_via_email/<%= @user.id %>/<%= @task.id %>"> Unsubscribe from this message </a>to stop recieving email notifications about this message thread<br/><br/>
  --<br/>
  <%=raw @footer_details%>
</body>
</html>